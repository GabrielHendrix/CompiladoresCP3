#include  <stdio.h>
#include <unistd.h>
#include <string.h>

int main (int argc, char *argv[])
{
    int i;
    i = 1;
    printf("%d", i);
    // int size = 16;
    // int data[size][size];
    // int value = 0;
	// int col = 0;
    // for (int row = 0; col < size; row++) {
    //    	for (col = 0; col < size; col++) {
	// 		data[row][col] = value;
	// 		value++;
    // 	}
    // }
}
